## Terminal
This directory is used to store configuration files for terminal applications on various platforms. The details are as follows:

*   `mac-os/`: Profile files for the Terminal application on macOS.
    -   `sonoma-tahoe-glass-dark.terminal`: A dark Terminal profile featuring a translucent, blurred background.
