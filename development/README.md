## Development
This directory is used to store configuration files and other resources for development tools and toolchains. The details are as follows:

-   `llvm/`: Resources for the LLVM project and its subprojects.
    -   `clang/`: Resources for the Clang compiler and its related tools.
        -   `clang-format/`: ClangFormat configuration files for C and C++ source code.
